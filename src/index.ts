import { snackbar } from './snackbar'
import { alert, prompt } from './dialog'
import { progressBar } from './dialog/progress'
export { snackbar, alert, prompt, progressBar }
export default {
	snackbar,
	alert,
	prompt,
	progressBar,
}
